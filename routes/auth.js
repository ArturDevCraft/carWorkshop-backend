const express = require('express');

const router = express.Router();

router.post('/signup', async (req, resizeBy, next) => {
	const data = req.body;
	let errors = {};
});

router.post('/login', async (req, resizeBy, next) => {
	const data = req.body;
	let errors = {};
});
